# ✅ Git репозиторий создан!

## 🎉 Что сделано:

1. ✅ Git инициализирован
2. ✅ Все файлы добавлены
3. ✅ Коммит создан
4. ✅ Frontend собран (`client/dist/`)

---

## 🚀 ЧТО ДЕЛАТЬ ДАЛЬШЕ:

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите "New repository" (зеленая кнопка)
3. Название: `toricy`
4. Описание: `Семейная социальная сеть`
5. **Public** или **Private** - на ваш выбор
6. **НЕ** добавляйте README, .gitignore, license (у нас уже есть)
7. Нажмите "Create repository"

### Шаг 2: Подключите репозиторий

GitHub покажет команды. Выполните:

```bash
git remote add origin https://github.com/ваш-username/toricy.git
git push -u origin main
```

**Замените `ваш-username` на ваш реальный username!**

Например:
```bash
git remote add origin https://github.com/alexey/toricy.git
git push -u origin main
```

### Шаг 3: Деплой на Netlify

#### Вариант А: Перетащить папку (проще всего!)

1. Откройте https://netlify.com
2. Войдите через GitHub
3. Нажмите "Add new site" → "Deploy manually"
4. Перетащите папку `client/dist/` в окно браузера
5. Готово! Скопируйте URL

#### Вариант Б: Через GitHub (автодеплой)

1. На Netlify: "Add new site" → "Import from Git"
2. Выберите репозиторий `toricy`
3. Настройки:
   - Base directory: `client`
   - Build command: `npm run build`
   - Publish directory: `client/dist`
4. Deploy!

### Шаг 4: Backend на Vercel

1. Откройте https://vercel.com
2. Войдите через GitHub
3. "Add New" → "Project"
4. Выберите `toricy`
5. Настройки:
   - Project Name: `toricy-backend`
   - Root Directory: `server`
   - Build Command: `npm install && npx prisma generate`
6. Environment Variables (добавьте пока без DATABASE_URL):
   ```
   JWT_SECRET=toricy-super-secret-key-2024
   EMAIL_HOST=smtp.gmail.com
   EMAIL_PORT=587
   EMAIL_USER=your-email@gmail.com
   EMAIL_PASSWORD=your-app-password
   GOOGLE_CLIENT_ID=your-google-client-id
   GOOGLE_CLIENT_SECRET=your-google-client-secret
   GOOGLE_CALLBACK_URL=https://ваш-backend.vercel.app/api/auth/google/callback
   ```
7. Deploy!

### Шаг 5: Добавьте Vercel Postgres (БЕСПЛАТНО!)

1. После деплоя откройте ваш проект на Vercel
2. Перейдите в "Storage" → "Create Database"
3. Выберите "Postgres"
4. Нажмите "Continue"
5. Название: `toricy-db`
6. Region: выберите ближайший
7. Нажмите "Create"
8. Vercel автоматически добавит `POSTGRES_URL` в Environment Variables!
9. Перейдите в Settings → Environment Variables
10. Добавьте новую переменную:
    ```
    DATABASE_URL=${POSTGRES_URL}
    ```
11. Redeploy проект (Deployments → три точки → Redeploy)

### Шаг 6: Обновите Google OAuth

1. https://console.cloud.google.com
2. Credentials → OAuth Client ID
3. Добавьте redirect URI:
   ```
   https://ваш-backend.vercel.app/api/auth/google/callback
   ```

---

## 📋 Команды для копирования:

### Подключить GitHub (замените username!):
```bash
git remote add origin https://github.com/ваш-username/toricy.git
git push -u origin main
```

---

## 📚 Полные инструкции:

- [ШПАРГАЛКА_ДЕПЛОЙ.md](./ШПАРГАЛКА_ДЕПЛОЙ.md) - Краткая шпаргалка
- [ДЕПЛОЙ_NETLIFY_VERCEL.md](./ДЕПЛОЙ_NETLIFY_VERCEL.md) - Полная инструкция
- [ГОТОВО_К_ДЕПЛОЮ.md](./ГОТОВО_К_ДЕПЛОЮ.md) - Выбор варианта

---

## ⚠️ ВАЖНО!

**Vercel НЕ поддерживает WebSocket!**

Если нужны чаты и звонки - используйте Render вместо Vercel:
- См. [ДЕПЛОЙ_ЗА_5_МИНУТ.md](./ДЕПЛОЙ_ЗА_5_МИНУТ.md)

---

## 💰 Стоимость: 0₽ навсегда! ✅

Всё бесплатно:
- Netlify: бесплатно
- Vercel: бесплатно
- Neon: бесплатно

---

## 🎯 Следующий шаг:

**Создайте репозиторий на GitHub и выполните команды выше!** 🚀
