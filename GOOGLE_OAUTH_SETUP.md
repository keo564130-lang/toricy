# Настройка Google OAuth для Toricy

## Пошаговая инструкция

### Шаг 1: Перейдите в Google Cloud Console

Откройте в браузере: **https://console.cloud.google.com**

### Шаг 2: Создайте новый проект

1. Нажмите на выпадающий список проектов вверху страницы
2. Нажмите **"Новый проект"** (New Project)
3. Введите название: **Toricy** (или любое другое)
4. Нажмите **"Создать"** (Create)
5. Дождитесь создания проекта (несколько секунд)
6. Выберите созданный проект из списка

### Шаг 3: Настройте экран согласия OAuth

1. В левом меню найдите **"APIs & Services"** → **"OAuth consent screen"**
2. Выберите **"External"** (Внешние пользователи)
3. Нажмите **"Create"** (Создать)

**Заполните форму:**
- **App name**: Toricy
- **User support email**: ваш email
- **Developer contact email**: ваш email
- Остальное можно оставить пустым
- Нажмите **"Save and Continue"** (Сохранить и продолжить)

**Scopes (области доступа):**
- Нажмите **"Add or Remove Scopes"**
- Выберите:
  - `.../auth/userinfo.email`
  - `.../auth/userinfo.profile`
- Нажмите **"Update"** → **"Save and Continue"**

**Test users (тестовые пользователи):**
- Нажмите **"Add Users"**
- Добавьте свой Gmail адрес
- Нажмите **"Save and Continue"**

### Шаг 4: Создайте OAuth 2.0 Client ID

1. В левом меню: **"APIs & Services"** → **"Credentials"**
2. Нажмите **"+ Create Credentials"** вверху
3. Выберите **"OAuth client ID"**

**Настройте приложение:**
- **Application type**: Web application
- **Name**: Toricy Web Client

**Authorized JavaScript origins:**
- Нажмите **"+ Add URI"**
- Добавьте: `http://localhost:5173`

**Authorized redirect URIs:**
- Нажмите **"+ Add URI"**
- Добавьте: `http://localhost:3000/api/auth/google/callback`

4. Нажмите **"Create"** (Создать)

### Шаг 5: Скопируйте учетные данные

После создания появится окно с:
- **Client ID** - длинная строка типа `123456789-abc...xyz.apps.googleusercontent.com`
- **Client Secret** - строка типа `your-google-client-secret...xyz`

**Скопируйте оба значения!**

### Шаг 6: Обновите файл .env

Откройте файл `server/.env` и замените:

```env
GOOGLE_CLIENT_ID="ваш-client-id-сюда"
GOOGLE_CLIENT_SECRET="ваш-client-secret-сюда"
```

На реальные значения, например:

```env
GOOGLE_CLIENT_ID="123456789-abcdefghijklmnop.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
```

### Шаг 7: Перезапустите сервер

Скажите мне "обнови сервера" или выполните:

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
cd server
npm run dev
```

### Шаг 8: Проверьте работу

1. Откройте http://localhost:5173
2. Нажмите "Регистрация"
3. Нажмите на кнопку **"Google"**
4. Вы будете перенаправлены на страницу входа Google
5. Выберите аккаунт и разрешите доступ
6. Вы автоматически войдете в Toricy!

## Важные замечания

### Для разработки (localhost)
- Приложение будет в режиме "Testing"
- Работает только для добавленных тестовых пользователей
- Это нормально для разработки

### Для продакшена (когда будете деплоить)
1. Измените redirect URI на ваш домен:
   - `https://yourdomain.com/api/auth/google/callback`
2. Обновите JavaScript origins:
   - `https://yourdomain.com`
3. Переведите приложение в режим "Production" в OAuth consent screen
4. Пройдите верификацию Google (если нужно)

## Возможные проблемы

### "redirect_uri_mismatch"
- Проверьте, что redirect URI точно совпадает: `http://localhost:3000/api/auth/google/callback`
- Убедитесь, что нет лишних слешей в конце

### "Access blocked: This app's request is invalid"
- Убедитесь, что добавили свой email в Test users
- Проверьте, что выбрали правильные scopes

### "Error 400: invalid_request"
- Проверьте, что Client ID и Secret правильно скопированы в .env
- Перезапустите сервер после изменения .env

## Быстрая ссылка

Прямая ссылка на создание credentials:
https://console.cloud.google.com/apis/credentials

---

**Готово!** После настройки пользователи смогут входить через Google одним кликом! 🎉
