# 🆓 ПОЛНОСТЬЮ БЕСПЛАТНЫЙ ДЕПЛОЙ

## ✅ 100% бесплатно, без карты!

### Вариант 1: Vercel (РЕКОМЕНДУЮ!)

**Всё в одном месте - frontend + backend + база!**

#### 1. Frontend на Vercel
```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой frontend
cd client
vercel
```

Или через сайт:
1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. "Add New Project"
4. Выберите репозиторий
5. Root Directory: `client`
6. Build Command: `npm run build`
7. Output Directory: `dist`
8. Deploy!

#### 2. Backend на Vercel (тоже бесплатно!)
1. Создайте файл `server/vercel.json`:
```json
{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}
```

2. Деплой:
```bash
cd server
vercel
```

#### 3. База данных - Neon (бесплатно!)
1. https://neon.tech
2. Создайте проект
3. Скопируйте connection string
4. Добавьте в Vercel Environment Variables

**Итого: 0₽ в месяц!** ✅

---

### Вариант 2: Netlify + Render + Neon

#### Frontend - Netlify (бесплатно)
```bash
cd client
npm run build
```
Загрузите `client/dist/` на https://netlify.com

#### Backend - Render (бесплатно!)
1. https://render.com
2. "New" → "Web Service"
3. Подключите GitHub
4. Root Directory: `server`
5. Build: `npm install && npx prisma generate`
6. Start: `npm start`
7. **Free tier** - выберите бесплатный план!

⚠️ Минус: засыпает после 15 минут неактивности, просыпается за ~30 секунд

#### База - Neon (бесплатно)
https://neon.tech - 0.5GB бесплатно

**Итого: 0₽ в месяц!** ✅

---

### Вариант 3: GitHub Pages + Cloudflare Workers

#### Frontend - GitHub Pages (бесплатно)
```bash
cd client
npm run build
```

Создайте `.github/workflows/deploy.yml`:
```yaml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: cd client && npm install && npm run build
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./client/dist
```

#### Backend - Cloudflare Workers (бесплатно!)
1. https://workers.cloudflare.com
2. 100,000 запросов/день бесплатно
3. Нужно переписать на Workers API (сложнее)

**Итого: 0₽ в месяц!** ✅

---

### Вариант 4: Railway (бесплатно с лимитами)

Railway дает **$5 кредитов в месяц** бесплатно!

1. https://railway.app
2. Войдите через GitHub
3. "New Project" → "Deploy from GitHub"
4. Выберите репозиторий
5. Добавьте PostgreSQL (бесплатно в рамках $5)
6. Деплой!

**Хватит на ~500 часов работы в месяц = бесплатно!** ✅

---

### Вариант 5: Fly.io (бесплатно)

```bash
# Установите Fly CLI
curl -L https://fly.io/install.sh | sh

# Деплой backend
cd server
fly launch
fly deploy

# Добавьте PostgreSQL
fly postgres create
```

**Бесплатный tier:**
- 3 VM по 256MB
- 3GB storage
- 160GB bandwidth

**Итого: 0₽ в месяц!** ✅

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ (самое простое)

### Netlify + Render + Neon = 100% БЕСПЛАТНО

1. **Frontend → Netlify**
   ```bash
   cd client
   npm run build
   ```
   Загрузите `client/dist/` на https://netlify.com

2. **Backend → Render**
   - https://render.com
   - Выберите FREE план
   - Подключите GitHub
   - Root: `server/`
   - Build: `npm install && npx prisma generate`
   - Start: `npm start`

3. **Database → Neon**
   - https://neon.tech
   - Создайте проект
   - Скопируйте connection string

**Минусы:**
- Render засыпает после 15 минут → первый запрос медленный (~30 сек)
- Но это БЕСПЛАТНО! 🎉

**Плюсы:**
- Полностью бесплатно
- Без карты
- Работает 24/7
- Автоматические деплои

---

## 📦 Что куда загружать

### Frontend (client/dist/)
- ✅ Netlify
- ✅ Vercel
- ✅ GitHub Pages
- ✅ Cloudflare Pages

### Backend (server/)
- ✅ Render (FREE план)
- ✅ Vercel
- ✅ Railway ($5 кредитов)
- ✅ Fly.io

### Database
- ✅ Neon (0.5GB бесплатно)
- ✅ Supabase (500MB бесплатно)
- ✅ Railway PostgreSQL (в рамках $5)
- ✅ Fly.io PostgreSQL

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Frontend на Netlify
```bash
cd client
npm run build
```
Перетащите `client/dist/` на https://netlify.com

### Шаг 2: Backend на Render
1. Зайдите на https://render.com
2. "New" → "Web Service"
3. Подключите GitHub
4. Root: `server`
5. Build: `npm install && npx prisma generate`
6. Start: `npm start`
7. **Выберите FREE план!**

### Шаг 3: База на Neon
1. https://neon.tech
2. Создайте проект
3. Скопируйте connection string
4. Добавьте в Render Environment Variables:
   ```
   DATABASE_URL=postgresql://...
   JWT_SECRET=your-secret
   EMAIL_USER=your-email@gmail.com
   EMAIL_PASSWORD=your-app-password
   GOOGLE_CLIENT_ID=...
   GOOGLE_CLIENT_SECRET=...
   ```

### Шаг 4: Обновите CORS
В `server/src/index.ts` добавьте URL Netlify:
```typescript
app.use(cors({ 
  origin: [
    'http://localhost:5173',
    'https://your-app.netlify.app'
  ], 
  credentials: true 
}));
```

### Шаг 5: Готово! 🎉
Ваше приложение работает на:
- Frontend: `https://your-app.netlify.app`
- Backend: `https://your-app.onrender.com`
- Database: Neon

**Стоимость: 0₽ в месяц!** ✅

---

## ⚠️ Важно знать про бесплатные планы

### Render FREE
- ✅ Бесплатно навсегда
- ⚠️ Засыпает после 15 минут неактивности
- ⚠️ Первый запрос медленный (~30 сек)
- ✅ Потом работает нормально

### Neon FREE
- ✅ 0.5GB storage
- ✅ 1 проект
- ✅ Достаточно для начала

### Netlify FREE
- ✅ 100GB bandwidth/месяц
- ✅ Автоматические деплои
- ✅ HTTPS бесплатно

---

## 💡 Советы

1. **Используйте Render FREE** - да, засыпает, но это бесплатно!
2. **Neon** - лучшая бесплатная PostgreSQL
3. **Netlify** - самый простой деплой frontend
4. **Если нужно чтобы не засыпал** - используйте UptimeRobot (бесплатно) для пинга каждые 5 минут

---

## 🎉 Итого

**Полностью бесплатная связка:**
- Frontend: Netlify
- Backend: Render (FREE)
- Database: Neon

**Стоимость: 0₽ в месяц навсегда!** 🚀

Никаких карт, никаких платежей, всё работает!
