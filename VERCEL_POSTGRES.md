# 🐘 Vercel Postgres (БЕСПЛАТНО!)

## ✅ Vercel предоставляет PostgreSQL бесплатно!

Забудьте про Neon, Supabase и другие сервисы.
Vercel Postgres - всё в одном месте!

---

## 🚀 Как добавить базу данных

### Шаг 1: Задеплойте backend на Vercel

Сначала задеплойте backend (без DATABASE_URL).

### Шаг 2: Создайте базу данных

1. Откройте ваш проект на Vercel
2. Перейдите во вкладку **"Storage"**
3. Нажмите **"Create Database"**
4. Выберите **"Postgres"**
5. Нажмите **"Continue"**

### Шаг 3: Настройте базу

1. **Database Name:** `toricy-db`
2. **Region:** выберите ближайший к вам
3. Нажмите **"Create"**

### Шаг 4: Подключите к проекту

1. Vercel автоматически добавит переменные:
   - `POSTGRES_URL`
   - `POSTGRES_PRISMA_URL`
   - `POSTGRES_URL_NON_POOLING`
   - и другие

2. Перейдите в **Settings → Environment Variables**

3. Добавьте новую переменную:
   ```
   DATABASE_URL=${POSTGRES_URL}
   ```
   
   Или используйте:
   ```
   DATABASE_URL=${POSTGRES_PRISMA_URL}
   ```

### Шаг 5: Redeploy

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите три точки → **"Redeploy"**
4. Готово!

---

## 💰 Бесплатный план

**Vercel Postgres Free:**
- ✅ 256 MB storage
- ✅ 60 часов compute time/месяц
- ✅ Достаточно для начала!

---

## 🔧 Миграции

После создания базы нужно запустить миграции.

### Вариант 1: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Подключитесь к проекту
vercel link

# Запустите миграции
vercel env pull .env.local
cd server
npx prisma migrate deploy
```

### Вариант 2: Добавьте в Build Command

В настройках проекта на Vercel:

**Build Command:**
```bash
npm install && npx prisma generate && npx prisma migrate deploy
```

⚠️ Но это может не сработать, так как база создается после деплоя.

### Вариант 3: Через Vercel Postgres Dashboard

1. Откройте Storage → ваша база
2. Перейдите в **"Data"** → **"Query"**
3. Скопируйте SQL из `server/prisma/migrations/*/migration.sql`
4. Выполните вручную

---

## 📊 Управление базой

### Через Vercel Dashboard

1. Storage → ваша база
2. **Data** - просмотр таблиц
3. **Query** - выполнение SQL
4. **Settings** - настройки

### Через Prisma Studio

```bash
cd server
npx prisma studio
```

---

## 🔄 Альтернативы (если нужно больше)

Если 256MB не хватит:

### Supabase (бесплатно)
- 500MB storage
- https://supabase.com

### Railway (бесплатно)
- $5 кредитов/месяц
- https://railway.app

### PlanetScale (бесплатно)
- 5GB storage
- https://planetscale.com

Но для начала Vercel Postgres более чем достаточно!

---

## ✅ Преимущества Vercel Postgres

1. ✅ Всё в одном месте (backend + база)
2. ✅ Автоматическая настройка переменных
3. ✅ Бесплатно
4. ✅ Быстрая настройка
5. ✅ Встроенный UI для управления

---

## 🎉 Готово!

Теперь у вас:
- Frontend на Netlify
- Backend на Vercel
- База данных на Vercel Postgres

Всё бесплатно! 🚀
