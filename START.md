# Как запустить Toricy

## ✅ Все уже настроено!

База данных PostgreSQL создана и миграции применены.

## Запуск приложения

### Вариант 1: Запустить все сразу (рекомендуется)

```bash
npm run dev
```

Это запустит:
- Backend на http://localhost:3000
- Frontend на http://localhost:5173

### Вариант 2: Запустить отдельно

**Терминал 1 - Backend:**
```bash
cd server
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd client
npm run dev
```

## Первый запуск

1. Откройте http://localhost:5173
2. Нажмите "Регистрация"
3. Выберите метод (Email или Телефон)
4. Заполните форму и создайте аккаунт
5. Готово! Можете пользоваться всеми функциями

## Доступные функции

✅ Лента постов с лайками и комментариями
✅ Чаты в реальном времени
✅ Видео/аудио звонки
✅ Голосовые сообщения
✅ Анимированные стикеры
✅ Группы и каналы
✅ Контакты
✅ Боты на Python
✅ Светлая/темная тема

## Google OAuth (опционально)

Чтобы включить вход через Google:

1. Перейдите на https://console.cloud.google.com
2. Создайте новый проект
3. Включите Google+ API
4. Создайте OAuth 2.0 credentials
5. Добавьте redirect URI: `http://localhost:3000/api/auth/google/callback`
6. Скопируйте Client ID и Client Secret
7. Обновите `server/.env`:
   ```
   GOOGLE_CLIENT_ID="your-google-client-id"
   GOOGLE_CLIENT_SECRET="your-google-client-secret"
   ```
8. Перезапустите сервер

## Создание бота на Python

1. Войдите в приложение
2. Перейдите в раздел "Боты"
3. Нажмите "Создать бота"
4. Введите имя и username
5. Добавьте Python код (пример ниже)
6. Нажмите "Создать"
7. Скопируйте токен бота
8. Нажмите "Запустить"

### Пример Python бота:

```python
import requests
import time

BOT_TOKEN = "ваш-токен-бота"
API_URL = "http://localhost:3000/api"

def send_message(chat_id, text):
    headers = {"Authorization": f"Bearer {BOT_TOKEN}"}
    data = {"chatId": chat_id, "content": text}
    response = requests.post(f"{API_URL}/messages", json=data, headers=headers)
    return response.json()

# Пример: отправка приветствия
while True:
    # Ваша логика бота
    time.sleep(60)
```

## Остановка приложения

Нажмите `Ctrl+C` в терминале

## Проблемы?

- Убедитесь, что PostgreSQL запущен: `sudo systemctl status postgresql`
- Проверьте порты 3000 и 5173 свободны
- Проверьте логи в терминале

Приятного использования! 🎉
