# 🚀 Деплой за 5 минут (БЕСПЛАТНО!)

## ✅ Полностью бесплатно, без карты!

### Шаг 1: Соберите frontend (1 минута)

```bash
cd client
npm run build
```

Готово! Папка `client/dist/` создана.

---

### Шаг 2: Загрузите frontend на Netlify (2 минуты)

1. Откройте https://netlify.com
2. Войдите через Google/GitHub
3. Нажмите "Add new site" → "Deploy manually"
4. Перетащите папку `client/dist/` в окно браузера
5. Готово! Скопируйте URL (например: `https://your-app.netlify.app`)

---

### Шаг 3: Создайте базу данных на Neon (1 минута)

1. Откройте https://neon.tech
2. Войдите через Google/GitHub
3. Нажмите "Create Project"
4. Скопируйте "Connection string" (начинается с `postgresql://`)

---

### Шаг 4: Загрузите backend на Render (1 минута)

#### Вариант А: Через GitHub (рекомендуется)

1. Загрузите код на GitHub:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/toricy.git
   git push -u origin main
   ```

2. Откройте https://render.com
3. Войдите через GitHub
4. Нажмите "New" → "Web Service"
5. Выберите ваш репозиторий
6. Настройки:
   - **Name:** toricy-backend
   - **Root Directory:** `server`
   - **Build Command:** `npm install && npx prisma generate`
   - **Start Command:** `npm start`
   - **Plan:** FREE ⚠️ ВАЖНО!
7. Нажмите "Advanced" → "Add Environment Variable"
8. Добавьте переменные:
   ```
   DATABASE_URL=postgresql://... (из Neon)
   JWT_SECRET=toricy-super-secret-key-2024
   EMAIL_HOST=smtp.gmail.com
   EMAIL_PORT=587
   EMAIL_USER=your-email@gmail.com
   EMAIL_PASSWORD=your-app-password
   GOOGLE_CLIENT_ID=your-google-client-id
   GOOGLE_CLIENT_SECRET=your-google-client-secret
   GOOGLE_CALLBACK_URL=https://ваш-backend.onrender.com/api/auth/google/callback
   ```
9. Нажмите "Create Web Service"
10. Подождите 2-3 минуты пока деплоится
11. Скопируйте URL (например: `https://toricy-backend.onrender.com`)

#### Вариант Б: Без GitHub (проще, но без автодеплоя)

К сожалению, Render требует GitHub. Используйте Вариант А.

---

### Шаг 5: Обновите Google OAuth (30 секунд)

1. Откройте https://console.cloud.google.com
2. Выберите ваш проект
3. APIs & Services → Credentials
4. Нажмите на ваш OAuth Client ID
5. В "Authorized redirect URIs" добавьте:
   ```
   https://ваш-backend.onrender.com/api/auth/google/callback
   ```
6. Сохраните

---

### Шаг 6: Обновите frontend (30 секунд)

Создайте файл `client/.env.production`:
```env
VITE_API_URL=https://ваш-backend.onrender.com
VITE_WS_URL=wss://ваш-backend.onrender.com
```

Пересоберите и загрузите:
```bash
cd client
npm run build
```

Перетащите `client/dist/` на Netlify снова (или настройте автодеплой через GitHub).

---

## ✅ Готово!

Ваше приложение работает:
- **Frontend:** https://ваш-app.netlify.app
- **Backend:** https://ваш-backend.onrender.com
- **Database:** Neon

**Стоимость: 0₽ в месяц!** 🎉

---

## ⚠️ Важно знать

### Render FREE план
- Засыпает после 15 минут неактивности
- Первый запрос медленный (~30 секунд)
- Потом работает нормально
- Это нормально для бесплатного плана!

### Как избежать засыпания (опционально)

Используйте UptimeRobot (бесплатно):
1. https://uptimerobot.com
2. Создайте монитор
3. URL: `https://ваш-backend.onrender.com/api/health`
4. Интервал: 5 минут
5. Готово! Backend не будет засыпать

---

## 🐛 Проблемы?

### Backend не запускается
- Проверьте логи на Render
- Убедитесь, что все переменные окружения добавлены
- Проверьте DATABASE_URL

### CORS ошибки
- Обновите `server/src/index.ts`:
  ```typescript
  app.use(cors({ 
    origin: [
      'http://localhost:5173',
      'https://ваш-app.netlify.app'
    ], 
    credentials: true 
  }));
  ```
- Передеплойте backend

### Google OAuth не работает
- Проверьте redirect URI в Google Console
- Должен быть: `https://ваш-backend.onrender.com/api/auth/google/callback`

---

## 🎯 Автоматический деплой (опционально)

### Netlify через GitHub

1. На Netlify: "Add new site" → "Import from Git"
2. Выберите репозиторий
3. Настройки:
   - **Base directory:** `client`
   - **Build command:** `npm run build`
   - **Publish directory:** `client/dist`
4. Environment variables:
   ```
   VITE_API_URL=https://ваш-backend.onrender.com
   VITE_WS_URL=wss://ваш-backend.onrender.com
   ```
5. Deploy!

Теперь при каждом push в GitHub:
- Frontend автоматически деплоится на Netlify
- Backend автоматически деплоится на Render

---

## 🎉 Поздравляю!

Ваша соцсеть работает в интернете!
Полностью бесплатно!
Можете делиться ссылкой с семьей! 🚀

**Стоимость: 0₽ навсегда!** ✅
